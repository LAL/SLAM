SLAM's todo-list
================

* web interface: allocate a new IP to a given host
* web interface for search, search by property, mac, IP, name, alias, etc.
* lists of hosts in pools and host_list with sort buttons (at least name, ip,
alias)
