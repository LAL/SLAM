"""This package handles all the processing required by the SLAM application,
interfaces should be built outside and interract with it."""

from slam import addrrange, generator, interface, log, models, views